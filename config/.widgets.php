<?php
/**
 * Файл конфигурации установленных виджетов.
 * 
 * Здесь перечисляются только системные виджеты (если они есть) с их параметрами 
 * конфигурации (некоторые параметры заполнять необязательно).
 * 
 * Пример объявления виджетов:
 * ```
 * return [
 *     'gm.wd.foobar' => [
 *         'lock'        => false, // системный (необязательно)
 *         'id'          => 'gm.wd.foobar', // уникальный идентификатор виджета
 *         'use'         => 'frontend', // назначение
 *         'rowId'       => 1, // уникальный идентификатор виджета в базе данных (необязательно)
 *         'enabled'     => true, // доступность
 *         'hasSettings' => true, // виджет имеет настройки (необязательно)
 *         'path'        => '/gm/gm.wd.foobar', // директория виджета
 *         'namespace'   => 'Gm\Widget\FooBar', // пространство имён виджета
 *         'name'        => 'Foo bar', // название (необязательно)
 *         'description' => 'Foo bar', // описание (необязательно)
 *         'version'     => '1.0' // версия (необязательно)
 *     ],
 *     // ...
 * ];
 * ```
 * 
 * @link https://gearmagic.ru
 * @copyright Copyright (c) 2015 Веб-студия GearMagic
 * @license https://gearmagic.ru/license/
 */
 
return [
];
